# Save exact versions for reproducible installs
save-exact=false
# Workspaces: allow local package linking
link-workspace-packages=true
# Prefer installing dev deps even inside workspaces
include-workspace-root=true
