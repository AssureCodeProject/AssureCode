# AssureCode Matchmaking Evaluation

> Generated by `tools/eval/matchmaking_eval.py` on 2026-08-04T23:51:52.
> Embedder: **all-MiniLM-L6-v2** (real sentence-transformers, not the hash-bucket `FakeEmbedder`). Seed 42.

## What is measured

24 client queries across 8 engineering domains are ranked against a synthetic freelancer pool. A freelancer is relevant iff their primary domain is the query's domain.

Queries come in two kinds and are reported separately:

- **explicit** — the client names the technologies ("Solidity, Ethers.js")
- **paraphrase** — the client describes the outcome and names none of them

A keyword matcher can serve the first. Only an embedding can serve the second, so the gap between the two rows is the measurement that distinguishes semantic matching from string overlap.

**Relevance rule.** A freelancer is relevant iff their primary domain equals the query's domain. Each freelancer also carries 2 skills from a secondary domain, which are counted as misses — so these numbers are a lower bound.

**Scope.** Synthetic pool and authored queries. Measures domain retrieval by the shipped pipeline, not real hiring outcomes.

---

## N = 100 freelancers

12–13 relevant freelancers per query, so R@10 cannot exceed **0.833**.

### Published weights (w_skill=0.5, w_trust=0.35, w_history=0.15)

| Query kind | P@1 | P@5 | P@10 | R@10 | MRR | nDCG@10 |
|---|---|---|---|---|---|---|
| overall | 0.583 | 0.592 | 0.479 | 0.383 | 0.736 | 0.516 |
| explicit | 0.750 | 0.700 | 0.575 | 0.459 | 0.865 | 0.623 |
| paraphrase | 0.250 | 0.375 | 0.287 | 0.230 | 0.479 | 0.302 |

### Skill term alone (w_skill=1.0)

| Query kind | P@1 | P@5 | P@10 | R@10 | MRR | nDCG@10 |
|---|---|---|---|---|---|---|
| overall | 0.625 | 0.650 | 0.600 | 0.479 | 0.748 | 0.615 |
| explicit | 0.812 | 0.775 | 0.700 | 0.558 | 0.906 | 0.736 |
| paraphrase | 0.250 | 0.400 | 0.400 | 0.321 | 0.431 | 0.372 |

### Weight ablation

All 231 settings of (w_skill, w_trust, w_history) on the simplex at step 0.05, scored on the same queries. The full grid is in `matchmaking_results.json`.

| Setting | w_skill | w_trust | w_history | nDCG@10 | P@5 | MRR |
|---|---|---|---|---|---|---|
| best | 0.95 | 0.0 | 0.05 | **0.6205** | 0.6583 | 0.7402 |
| shipped | 0.5 | 0.35 | 0.15 | 0.5161 | 0.5917 | 0.736 |

The shipped weights rank **44 of 231**.

### Latency

| | ms |
|---|---|
| Model load (first embed in the process) | 6952 |
| Cold first query (embeds 100 profiles one at a time) | 3649 |
| Warm query, mean over 23 | 65.3 |
| Warm query, p50 | 60.6 |
| Warm query, p95 | 102.3 |

`Matchmaker` embeds profile vectors lazily and caches them, so the first query of a process pays N sequential model calls. Reporting only the warm figure would hide that.

---

## N = 1000 freelancers

125–125 relevant freelancers per query, so R@10 cannot exceed **0.080**.

### Published weights (w_skill=0.5, w_trust=0.35, w_history=0.15)

| Query kind | P@1 | P@5 | P@10 | R@10 | MRR | nDCG@10 |
|---|---|---|---|---|---|---|
| overall | 0.625 | 0.667 | 0.638 | 0.051 | 0.731 | 0.644 |
| explicit | 0.750 | 0.837 | 0.831 | 0.067 | 0.854 | 0.829 |
| paraphrase | 0.375 | 0.325 | 0.250 | 0.020 | 0.484 | 0.275 |

### Skill term alone (w_skill=1.0)

| Query kind | P@1 | P@5 | P@10 | R@10 | MRR | nDCG@10 |
|---|---|---|---|---|---|---|
| overall | 0.750 | 0.708 | 0.704 | 0.056 | 0.798 | 0.712 |
| explicit | 0.938 | 0.925 | 0.906 | 0.073 | 0.969 | 0.915 |
| paraphrase | 0.375 | 0.275 | 0.300 | 0.024 | 0.458 | 0.306 |

### Weight ablation

All 231 settings of (w_skill, w_trust, w_history) on the simplex at step 0.05, scored on the same queries. The full grid is in `matchmaking_results.json`.

| Setting | w_skill | w_trust | w_history | nDCG@10 | P@5 | MRR |
|---|---|---|---|---|---|---|
| best | 0.95 | 0.05 | 0.0 | **0.7145** | 0.7167 | 0.7993 |
| shipped | 0.5 | 0.35 | 0.15 | 0.6438 | 0.6667 | 0.7309 |

The shipped weights rank **66 of 231**.

### Latency

| | ms |
|---|---|
| Model load (first embed in the process) | 7270 |
| Cold first query (embeds 1000 profiles one at a time) | 36585 |
| Warm query, mean over 23 | 84.7 |
| Warm query, p50 | 74.0 |
| Warm query, p95 | 108.4 |

`Matchmaker` embeds profile vectors lazily and caches them, so the first query of a process pays N sequential model calls. Reporting only the warm figure would hide that.

---

## Findings

1. **The explicit/paraphrase gap is large and holds at both pool sizes.** When the client names the technology the ranking is strong; when they describe the outcome in plain language it degrades sharply. The system is closer to a robust keyword matcher than to a semantic one.
2. **The trust and history terms cost retrieval accuracy.** Weighting the semantic term alone beats the shipped weights on every headline metric, and the ablation optimum sits near w_skill = 0.95. Trust and delivery count are properties of the freelancer, not of the query, so they can only reorder — never sharpen — a domain match.
3. That is an argument about *this* objective, not about the weights being wrong. Trust is in the score on purpose: the product ranks by who should be hired, not by who is most textually similar. What the ablation establishes is that the current split has never been measured against either goal, and that no logged hiring outcome exists in this repo to measure the first one against.

*Regenerate with `python tools/eval/matchmaking_eval.py`.*
